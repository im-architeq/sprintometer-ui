/**
 * App Information
 */
export const AppInfo = {
  name: 'Sprintometer',
  root: '/a/sprintometer-app',
};

/**
 * Routes
 */
export const Routes = {
  community: `${AppInfo.root}`,
  development: `${AppInfo.root}/development`,
};
